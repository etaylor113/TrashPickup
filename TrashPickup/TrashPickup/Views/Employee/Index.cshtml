@model IEnumerable<TrashPickup.Models.ApplicationUser>
@using Microsoft.AspNet.Identity
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Welcome @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().FirstName!</h2>

<h4>This is the employee menu.</h4>

<p>
    @Html.ActionLink("View Map", "MapView")
</p>

<div class="form-group">
    @Html.Label("Select Day", new { @class = "col-md-2 control-label" })
    <div class="col-md-10">
        @Html.DropDownList("DayOfWeek", new SelectList(new[] { "Monday", "Tuesday", "Wednesday", "Thursday", "Friday"}))
    </div>
</div>

    
<div class="form-group">
    <div class="col-md-offset-2 col-md-10">
        <input type="submit" class="btn btn-default" value="Submit" />
    </div>
</div>

<p>
    @Html.ActionLink("View my Customers", "EmployeeCustomersView")
</p>

<table class="table">
    <tr>
        <th>
            First Name
        </th>
        <th>
            Last Name
        </th>
        <th>
            Address
        </th>
        <th>
            Area Code
        </th>
        <th>
            Role
        </th>
        <th>
            Day of Week
        </th>
        <th>
            Email
        </th>
        <th>
            Email Confirmed
        </th>
        <th>
            Phone Number
        </th>
        <th>
            Phone Number Confirmed
        </th>
        <th>
            Username
        </th>
        <th></th>
    </tr>

   
        <tr>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().FirstName
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().LastName
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().Address
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().AreaCode
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().UserRoles
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().DayOfWeek
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().Email
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().EmailConfirmed
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().PhoneNumber
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().PhoneNumberConfirmed
            </td>
            <td>
                @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().UserName
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit") 
                @Html.ActionLink("Details", "Details") 
                @Html.ActionLink("Delete", "Delete")
            </td>
        </tr>
    

</table>



